#include <iostream>
using namespace std;

int main(int argc, char *argv[]) {
	float a,b,c;
	do
	{
		cout<<"Ingrese el primer lado:";
		cin>>a;
	} while(a<=0);
	do
	{
		cout<<"Ingrese el segundo lado:";
		cin>>b;
	} while(b<=0);
	do
	{
		cout<<"Ingrese el tercer lado:";
		cin>>c;
	} while(c<=0);
	//algoritmo para saber si es determinado triangulo
	return 0;
}

